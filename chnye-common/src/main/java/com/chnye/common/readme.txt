able
  代表着一种能力。
  如IListenable<T>{
      void addListener( T listener );
      void removeListener( T listener );
      void clear();
    }
  这里的T是监听器。Listenable表示具有被监听的能力。
   
callback
  回调
  重点的概念有两个： 一个是回调的过程方法，再一个就是回调的结果。
consumer

dispatch

enumentity

eventdispatch

filter

fsm

function

handler

predicate

processor

producer

runnable

tuple

utils
